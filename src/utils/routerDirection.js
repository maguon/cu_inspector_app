// import { Actions } from 'react-native-router-flux'

// export const peccancyInfo = previousViewName => {
//     if (previousViewName === 'home') return () => Actions.peccancyInfoAtHomeBlock({ previousViewName })
// }
